def validAnagram(first, second):
    firstLen = len(first)
    secondLen = len(second)
    
    if not firstLen == secondLen : return False
    
    myString = ''
    for i in range(firstLen):
        


first = "anagram"
second = "nagaram"
validAnagram(first, second)
